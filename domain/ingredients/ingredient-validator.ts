import { Ingredient, IngredientCategory, IngredientUnit } from "./ingredient.types";
import { normalizeIngredientText } from "./ingredient-normalizer";
const validUnits = new Set<IngredientUnit>(["g","kg","ml","l","tsp","tbsp","cup","piece","pinch","clove","slice","can"]);
export function validateIngredientDataset(ingredients: Ingredient[], categories: IngredientCategory[]): string[] { const issues:string[]=[]; const ids=new Set<string>(),slugs=new Set<string>(),aliases=new Map<string,string>(),categoryIds=new Set(categories.map(c=>c.id)),canonicalNames=new Map(ingredients.map(i=>[normalizeIngredientText(i.name),i.id])); for(const ingredient of ingredients){if(!ingredient.id||!ingredient.slug||!ingredient.name)issues.push(`missing required field: ${ingredient.id||ingredient.name||"unknown"}`);if(ids.has(ingredient.id))issues.push(`duplicate id: ${ingredient.id}`);ids.add(ingredient.id);if(slugs.has(ingredient.slug))issues.push(`duplicate slug: ${ingredient.slug}`);slugs.add(ingredient.slug);if(!categoryIds.has(ingredient.categoryId))issues.push(`unknown category: ${ingredient.categoryId} on ${ingredient.id}`);if(ingredient.defaultUnit&&!validUnits.has(ingredient.defaultUnit))issues.push(`invalid unit: ${ingredient.defaultUnit} on ${ingredient.id}`);if(/^\d|\b(chopped|diced|minced|grated|boiled)\b/i.test(ingredient.name))issues.push(`identity contains quantity or preparation: ${ingredient.name}`);for(const term of ingredient.aliases){const key=normalizeIngredientText(term);if(key===normalizeIngredientText(ingredient.name))issues.push(`redundant alias: ${term}`);const canonicalOwner=canonicalNames.get(key);if(canonicalOwner&&canonicalOwner!==ingredient.id)issues.push(`alias conflicts with canonical name: ${term} (${canonicalOwner}, ${ingredient.id})`);const owner=aliases.get(key);if(owner&&owner!==ingredient.id)issues.push(`alias collision: ${term} (${owner}, ${ingredient.id})`);aliases.set(key,ingredient.id);}} return issues; }
