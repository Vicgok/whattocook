import { Ingredient, PantryItem, RecipeIngredient, RecipeMatchResult } from "./ingredient.types";
export function matchRecipeToPantry(pantryItems: PantryItem[], recipeIngredients: RecipeIngredient[], ingredients: Ingredient[]): RecipeMatchResult { const byId=new Map(ingredients.map(i=>[i.id,i])); const pantryIds=new Set(pantryItems.map(item=>item.ingredientId)); const satisfies=(recipeId:string)=>{if(pantryIds.has(recipeId))return true; const recipe=byId.get(recipeId); if(!recipe)return false; for(const pantryId of pantryIds){const pantry=byId.get(pantryId); if(pantry?.parentIngredientId===recipeId)return true;} return false;}; const matchedIngredients:RecipeIngredient[]=[],missingIngredients:RecipeIngredient[]=[],optionalMissingIngredients:RecipeIngredient[]=[]; for(const item of recipeIngredients){if(satisfies(item.ingredientId))matchedIngredients.push(item);else if(item.isOptional)optionalMissingIngredients.push(item);else missingIngredients.push(item);} const totalRequired=recipeIngredients.filter(i=>!i.isOptional).length; const matchedRequired=matchedIngredients.filter(i=>!i.isOptional).length; const matchPercentage=totalRequired===0?100:Math.round((matchedRequired/totalRequired)*100); const matchTier=matchPercentage===100?"ready":matchPercentage>=90?"high":matchPercentage>=75?"good":matchPercentage>=50?"partial":"low"; return {matchedIngredients,missingIngredients,optionalMissingIngredients,matchedRequiredCount:matchedRequired,totalRequiredCount:totalRequired,matchPercentage,canCook:missingIngredients.length===0,matchTier}; }
